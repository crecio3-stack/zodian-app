import { listZodianCanonicalIdentityCohort12V2, type ZodianCohort12V2Entry } from "./zodian-canonical-identity-cohort-12-v2.ts";
export const ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_APPROVED="zodian-canonical-identity-cohort-12-v2-approved" as const;
export const ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_APPROVAL_COMMIT="3ea47c3a7462f7ebc4219c40e99ef2c46ac117c2" as const;
export function canonicalizeZodianCanonicalIdentityCohort12V2(entries:readonly ZodianCohort12V2Entry[]=listZodianCanonicalIdentityCohort12V2()){return JSON.stringify({version:ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_APPROVED,entries:entries.map(e=>({identity:e.identity,status:e.status,approvalEvidence:e.approvalEvidence,provenance:e.profile?.provenance?.sourceIds,noteCounts:e.profile?[e.profile.coreMotivations.length,e.profile.recurringStrengths.length,e.profile.recurringFriction.length,e.profile.commonBlindSpots.length,e.profile.interpersonalPatterns.length,e.profile.emotionalPatterns.length]:[],profile:e.profile}))});}
export async function fingerprintZodianCanonicalIdentityCohort12V2(entries?:readonly ZodianCohort12V2Entry[]){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(canonicalizeZodianCanonicalIdentityCohort12V2(entries)));return[...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,"0")).join("");}
export const ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_FINGERPRINT=await fingerprintZodianCanonicalIdentityCohort12V2();
