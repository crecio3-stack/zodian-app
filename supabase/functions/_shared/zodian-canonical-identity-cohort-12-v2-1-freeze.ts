import { listZodianCanonicalIdentityCohort12V2_1, ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_1 } from "./zodian-canonical-identity-cohort-12-v2-1.ts";
export const canonicalizeZodianCanonicalIdentityCohort12V2_1 = () => JSON.stringify({ version: ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_1, entries: listZodianCanonicalIdentityCohort12V2_1().map((e) => ({ identity: e.identity, status: e.status, approvalEvidence: e.approvalEvidence, provenance: e.profile?.provenance?.sourceIds, noteCounts: e.profile ? [e.profile.coreMotivations.length,e.profile.recurringStrengths.length,e.profile.recurringFriction.length,e.profile.commonBlindSpots.length,e.profile.interpersonalPatterns.length,e.profile.emotionalPatterns.length] : [], profile: e.profile })) });
export async function fingerprintZodianCanonicalIdentityCohort12V2_1(){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(canonicalizeZodianCanonicalIdentityCohort12V2_1()));return[...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,"0")).join("");}
export const ZODIAN_CANONICAL_IDENTITY_COHORT_12_V2_1_FINGERPRINT=await fingerprintZodianCanonicalIdentityCohort12V2_1();
